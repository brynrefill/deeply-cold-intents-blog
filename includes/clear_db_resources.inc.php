<?php
    //if(!pg_free_result($result)) echo "Error on free the memory!";
    
    //Using pg_close() is not usually necessary, as non-persistent open connections are automatically closed at the end of the script. And the same for free_result(?)))))))((((????))))
    //pg_close($dbconn);
?>